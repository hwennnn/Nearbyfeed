import { styled } from 'nativewind';
import { KeyboardAvoidingView as NKeyboardAvoidingView } from 'react-native';
import { KeyboardAwareScrollView } from 'react-native-keyboard-aware-scroll-view';

export const KeyboardAvoidingView = styled(NKeyboardAvoidingView);
export const KeyboardAvoidingScrollView = styled(KeyboardAwareScrollView);
